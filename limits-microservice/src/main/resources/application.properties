spring.application.name=limits-service
spring.config.import=optional:configserver:http://localhost:8888

spring.cloud.config.profile=qa

# that doesn't matter because we are fetching data through the spring-cloud-config-server that is fetching through the git repository

